[package]
name = "learn-bevy-shader"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["free_camera"] }

[dev-dependencies]
bevy = { version = "0.18.1", features = ["dynamic_linking", "free_camera"] }
